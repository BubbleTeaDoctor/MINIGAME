无限制角斗场 v0.9.0

新增：本地文件夹工作区模式
- 现在可以在 Index / Editor 里点击“选择本地文件夹”
- 选中后会在该文件夹内自动维护：
  - rulesets.json
  - active.json
- 仍然兼容原来的 LocalStorage 命名空间模式

建议
- 使用 Chrome / Edge
- 最好通过 localhost 打开，不要直接双击 file://
- 第一次选择文件夹时，浏览器会请求读写权限

说明
- 切换到文件夹模式时，会把当前规则库复制过去
- 恢复默认存储时，会把当前规则库复制回浏览器默认存储
